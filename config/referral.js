module.exports = {
  SNC_BONUSES: {
    1: 200 * Math.pow(10, 8),
    50: 100 * Math.pow(10, 8),
  },
  SLC_BONUSES:{
    1: 300 * Math.pow(10, 8),
    50: 200 * Math.pow(10, 8),
  },
  REF_BONUS: 100 * Math.pow(10, 8),
  COINBASE_PRIVATE_KEY: ''
};
